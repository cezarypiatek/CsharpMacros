namespace CsharpMacros.Test.Macros.Properties.TestData
{
    class UserDTO
    {
        public string FirstName { get; set; }
        public string LastName { get; set; }
        public int Age { get; set; }

        public int DoSth1(int a)
        {
            return 1;
        }
        
        public string DoSth2(string b)
        {
            return "";
        }
    }

    class _001_ObjectWithOwnMethods
    {

        public void DoSth()
        {
            var user = new UserDTO();
            //[|macro(x in methods(UserDTO))|]
            //var paramFor${x.name} = default(${x.paramType1});
            //${x.returnType} resultOf${x.name} = user.${x.name}(paramFor${x.name});
        }
    }
}
